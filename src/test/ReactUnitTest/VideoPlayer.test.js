import VideoPlayer from "../../Presentation/components/VideoPlayer";
import {fireEvent, render, screen, waitFor} from "@testing-library/react";

describe("test VideoPlayer",  () => {

    test( "", () => {

    });
    /*
    const handleClick = jest.fn();
    render(<VideoButton onClick={handleClick} isPlaying={true} />);
    const playBtn = screen.getByRole('button');
    const img = screen.getByAltText('Button');
    fireEvent.click(playBtn);
    await waitFor(() => expect(img.src).toBe("http://localhost/pause_button.svg"));

     */
});